import styles from "./LoginPage.module.css";

export default function LoginPage() {
  return (
    <div className={styles.container}>
      <h1>Login</h1>
      {/* Тут позже будет форма логіна (Formik + Yup) */}
    </div>
  );
}
