"use client";

import { ReactNode } from "react";

export default function Modal({ children }: { children: ReactNode }) {
  return (
    <div>
      {/* TODO: Modal layout */}
      {children}
    </div>
  );
}
